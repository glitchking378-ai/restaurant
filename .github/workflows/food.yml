name: Food System

on:
  workflow_dispatch:

jobs:
  food:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Food pipeline
        run: echo "Food is being processed through the system."

      - name: Serve food
        run: echo "Order served to table."
