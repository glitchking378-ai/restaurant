name: Waiter Bot

on:
  workflow_dispatch:
  push:
    paths:
      - 'waiter.js'

jobs:
  waiter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Waiter online
        run: echo "Waiter is ready to take orders."

      - name: Serve greeting
        run: echo "Welcome to our restaurant!"
